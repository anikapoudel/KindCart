import 'package:flutter/material.dart';
import 'package:kindcart/screens/adddonation_screen.dart';
import 'package:url_launcher/url_launcher.dart';
import 'auth_screen.dart'; // Make sure to import these screens
import 'donate_screen.dart';
import 'shop_screen.dart';

class AboutScreen extends StatelessWidget {
  const AboutScreen({super.key});

  Future<void> _launchEmail(String email) async {
    final Uri emailUri = Uri(
      scheme: 'mailto',
      path: email,
    );

    if (!await launchUrl(emailUri)) {
      throw 'Could not open email app';
    }
  }

  Future<void> _launchPhone(String phone) async {
    final Uri phoneUri = Uri(
      scheme: 'tel',
      path: phone,
    );

    if (!await launchUrl(phoneUri)) {
      throw 'Could not open phone dialer';
    }
  }

  Future<void> _launchMaps(String address) async {
    final Uri mapUri = Uri(
      scheme: 'https',
      host: 'www.google.com',
      path: '/maps/search/',
      queryParameters: {'query': address},
    );

    if (!await launchUrl(mapUri)) {
      throw 'Could not open maps';
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('About KindCart'),
        leading: IconButton(
          icon: const Icon(Icons.arrow_back),
          onPressed: () {
            Navigator.pop(context);
          },
        ),
      ),
      body: SingleChildScrollView(
        padding: const EdgeInsets.all(20),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            // Logo and Title
            Center(
              child: Column(
                children: [
                  Image.asset(
                    'assets/logo.png',
                    width: MediaQuery.of(context).size.width * 0.8,
                    fit: BoxFit.contain,
                  ),
                  const SizedBox(height: 10),
                  Text(
                    'KindCart',
                    style: TextStyle(
                      fontSize: 32,
                      fontWeight: FontWeight.bold,
                      color: Colors.green[700],
                    ),
                  ),
                  const Text(
                    'Sustainable Shopping & Donations',
                    style: TextStyle(
                      fontSize: 16,
                      color: Colors.grey,
                    ),
                  ),
                ],
              ),
            ),

            const SizedBox(height: 30),

            // Mission Section
            const Text(
              'Our Mission',
              style: TextStyle(
                fontSize: 24,
                fontWeight: FontWeight.bold,
              ),
            ),
            const SizedBox(height: 10),
            const Text(
              'To enable purchasing and selling second-hand items while facilitating '
                  'easier donations of useful items. The app aims to encourage sustainable '
                  'consumption and social responsibility through a user-friendly technological '
                  'interface. The goal is not only to create a marketplace platform but also '
                  'to foster a community that practices sustainable and responsible consumption, '
                  'along with charitable donations focusing on ethical fashion and giving.',
              style: TextStyle(fontSize: 16, height: 1.5),
            ),

            const SizedBox(height: 25),

            // What We Do
            const Text(
              'What We Do',
              style: TextStyle(
                fontSize: 24,
                fontWeight: FontWeight.bold,
              ),
            ),
            const SizedBox(height: 15),

            _buildFeatureCard(
              icon: Icons.shopping_bag,
              title: 'Thrift Shopping',
              description: 'Buy quality second-hand items at affordable prices.',
              color: Colors.green,
            ),

            _buildFeatureCard(
              icon: Icons.volunteer_activism,
              title: 'Easy Donations',
              description: 'Donate items directly to people who need them.',
              color: Colors.blue,
            ),

            _buildFeatureCard(
              icon: Icons.eco,
              title: 'Reduce Waste',
              description: 'Extend product lifespan and reduce landfill waste.',
              color: Colors.orange,
            ),

            _buildFeatureCard(
              icon: Icons.people,
              title: 'Community Support',
              description: 'Connect donors with local charities and individuals.',
              color: Colors.purple,
            ),

            const SizedBox(height: 25),

            // Stats
            const Text(
              'Our Impact',
              style: TextStyle(
                fontSize: 24,
                fontWeight: FontWeight.bold,
              ),
            ),
            const SizedBox(height: 15),

            Row(
              mainAxisAlignment: MainAxisAlignment.spaceAround,
              children: [
                _buildStatCard('500+', 'Items Saved', Icons.recycling),
                _buildStatCard('200+', 'Happy Donors', Icons.volunteer_activism),
                _buildStatCard('300+', 'Families Helped', Icons.family_restroom),
              ],
            ),

            const SizedBox(height: 25),

            // Team Section
            const Text(
              'Meet Our Team',
              style: TextStyle(
                fontSize: 24,
                fontWeight: FontWeight.bold,
              ),
            ),
            const SizedBox(height: 10),
            const Text(
              'We are a passionate group of developers, designers, and sustainability '
                  'enthusiasts dedicated to making a positive impact.',
              style: TextStyle(fontSize: 16, height: 1.5),
            ),

            const SizedBox(height: 25),

            // Contact Info
            Card(
              child: Padding(
                padding: const EdgeInsets.all(20),
                child: Column(
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: [
                    const Text(
                      'Contact Us',
                      style: TextStyle(
                        fontSize: 20,
                        fontWeight: FontWeight.bold,
                      ),
                    ),
                    const SizedBox(height: 15),

                    // Email - opens email app
                    InkWell(
                      onTap: () {
                        _launchEmail('panika22@tbc.edu.np');
                      },
                      child: _buildContactInfo(Icons.email, 'panika22@tbc.edu.np'),
                    ),
                    const SizedBox(height: 10),

                    // Phone - opens phone dialer
                    InkWell(
                      onTap: () {
                        _launchPhone('+977-9867545678');
                      },
                      child: _buildContactInfo(Icons.phone, '+977-9867545678'),
                    ),
                    const SizedBox(height: 10),

                    // Location - opens maps
                    InkWell(
                      onTap: () {
                        _launchMaps('Thapathali Road, Kathmandu 44600');
                      },
                      child: _buildContactInfo(Icons.location_on, 'Thapathali Road, Kathmandu 44600'),
                    ),
                  ],
                ),
              ),
            ),

            const SizedBox(height: 30),

            // Call to Action - Join Our Mission Button
            Center(
              child: ElevatedButton.icon(
                icon: const Icon(Icons.handshake),
                label: const Text('Join Our Mission'),
                onPressed: () {
                  _showJoinOptions(context);
                },
                style: ElevatedButton.styleFrom(
                  padding: const EdgeInsets.symmetric(horizontal: 30, vertical: 15),
                  backgroundColor: Colors.green[700],
                  foregroundColor: Colors.white,
                ),
              ),
            ),

            const SizedBox(height: 40),
          ],
        ),
      ),
    );
  }

  // Method to show join options
  void _showJoinOptions(BuildContext context) {
    showModalBottomSheet(
      context: context,
      isScrollControlled: true,
      shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.vertical(top: Radius.circular(20)),
      ),
      builder: (BuildContext context) {
        return SingleChildScrollView(
          child: Container(
            padding: const EdgeInsets.all(20),
            child: Column(
              mainAxisSize: MainAxisSize.min,
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Center(
                  child: Container(
                    width: 40,
                    height: 4,
                    decoration: BoxDecoration(
                      color: Colors.grey[300],
                      borderRadius: BorderRadius.circular(2),
                    ),
                  ),
                ),
                const SizedBox(height: 20),
                const Text(
                  'Join Our Mission',
                  style: TextStyle(
                    fontSize: 22,
                    fontWeight: FontWeight.bold,
                  ),
                ),
                const SizedBox(height: 10),
                const Text(
                  'Choose how you would like to contribute:',
                  style: TextStyle(
                    fontSize: 16,
                    color: Colors.grey,
                  ),
                ),
                const SizedBox(height: 20),

                // Option 1: Sign Up
                ListTile(
                  leading: Container(
                    padding: const EdgeInsets.all(10),
                    decoration: BoxDecoration(
                      color: Colors.green.withAlpha(30),
                      borderRadius: BorderRadius.circular(10),
                    ),
                    child: const Icon(Icons.person_add, color: Colors.green),
                  ),
                  title: const Text('Create an Account'),
                  subtitle: const Text('Sign up to start donating or shopping'),
                  trailing: const Icon(Icons.arrow_forward_ios, size: 16),
                  onTap: () {
                    Navigator.pop(context); // Close bottom sheet
                    // Navigate to Sign Up Screen
                    Navigator.push(
                      context,
                      MaterialPageRoute(
                        builder: (context) => const AuthScreen(),
                      ),
                    );
                  },
                ),

                const Divider(),

                // Option 2: Start Donating
                ListTile(
                  leading: Container(
                    padding: const EdgeInsets.all(10),
                    decoration: BoxDecoration(
                      color: Colors.blue.withAlpha(30),
                      borderRadius: BorderRadius.circular(10),
                    ),
                    child: const Icon(Icons.volunteer_activism, color: Colors.blue),
                  ),
                  title: const Text('Start Donating'),
                  subtitle: const Text('Donate items you no longer need'),
                  trailing: const Icon(Icons.arrow_forward_ios, size: 16),
                  onTap: () {
                    Navigator.pop(context); // Close bottom sheet
                    // Navigate to Donation Screen
                    Navigator.push(
                      context,
                      MaterialPageRoute(
                        builder: (context) => const AddDonationScreen(),
                      ),
                    );
                  },
                ),

                const Divider(),

                // Option 3: Shop Sustainably
                ListTile(
                  leading: Container(
                    padding: const EdgeInsets.all(10),
                    decoration: BoxDecoration(
                      color: Colors.orange.withAlpha(30),
                      borderRadius: BorderRadius.circular(10),
                    ),
                    child: const Icon(Icons.shopping_bag, color: Colors.orange),
                  ),
                  title: const Text('Shop Sustainably'),
                  subtitle: const Text('Browse eco-friendly second-hand items'),
                  trailing: const Icon(Icons.arrow_forward_ios, size: 16),
                  onTap: () {
                    Navigator.pop(context); // Close bottom sheet
                    // Navigate to Shopping Screen
                    Navigator.push(
                      context,
                      MaterialPageRoute(
                        builder: (context) => const ShopScreen(),
                      ),
                    );
                  },
                ),

                const SizedBox(height: 20),

                // Close button
                SizedBox(
                  width: double.infinity,
                  child: OutlinedButton(
                    onPressed: () => Navigator.pop(context),
                    child: const Text('Maybe Later'),
                  ),
                ),
                const SizedBox(height: 10), // Extra padding for safety
              ],
            ),
          ),
        );
      },
    );
  }

  Widget _buildFeatureCard({
    required IconData icon,
    required String title,
    required String description,
    required Color color,
  }) {
    return Card(
      margin: const EdgeInsets.only(bottom: 15),
      child: Padding(
        padding: const EdgeInsets.all(15),
        child: Row(
          children: [
            Container(
              padding: const EdgeInsets.all(12),
              decoration: BoxDecoration(
                color: color.withAlpha(20),
                borderRadius: BorderRadius.circular(10),
              ),
              child: Icon(icon, color: color, size: 30),
            ),
            const SizedBox(width: 15),
            Expanded(
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Text(
                    title,
                    style: TextStyle(
                      fontSize: 18,
                      fontWeight: FontWeight.w600,
                      color: color,
                    ),
                  ),
                  const SizedBox(height: 5),
                  Text(
                    description,
                    style: const TextStyle(
                      fontSize: 14,
                      color: Colors.grey,
                    ),
                  ),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }

  Widget _buildStatCard(String value, String label, IconData icon) {
    return Column(
      children: [
        Container(
          padding: const EdgeInsets.all(15),
          decoration: BoxDecoration(
            color: Colors.green.withAlpha(20),
            shape: BoxShape.circle,
          ),
          child: Icon(icon, color: Colors.green, size: 30),
        ),
        const SizedBox(height: 10),
        Text(
          value,
          style: const TextStyle(
            fontSize: 20,
            fontWeight: FontWeight.bold,
          ),
        ),
        Text(
          label,
          style: const TextStyle(
            fontSize: 12,
            color: Colors.grey,
          ),
          textAlign: TextAlign.center,
        ),
      ],
    );
  }

  Widget _buildContactInfo(IconData icon, String text) {
    return Row(
      children: [
        Icon(icon, color: Colors.green),
        const SizedBox(width: 10),
        Text(
          text,
          style: const TextStyle(fontSize: 16),
        ),
      ],
    );
  }
}
